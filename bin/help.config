#!/usr/bin/env bash
set -euo pipefail

cat <<-'EOS'
Environment variables:

GO_SDK_PATH - the directory where the Go SDK will be installed (default is $HOME/sdk)
GO_LOW_LIMIT_VERSION - the lower limit of the version displayed by list-all (default is 1.12.0)

EOS
